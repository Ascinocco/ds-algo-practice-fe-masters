export default function bs_list(haystack: number[], needle: number): boolean {
  // we want to cut the array in half each time so that we can get closer to the value until we eventually find it.
  // this only works with ordered data sets
}
