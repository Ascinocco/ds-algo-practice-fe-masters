export default function bs_list(haystack: number[], needle: number): boolean {
//  let lo = 0
//  let hi = haystack.length

//  while (lo < hi) {
    // find our midpoint
//    const mid = Math.floor(lo + (hi - lo) / 2);
    
    // get our mid point value
//    const v = haystack[mid]

    // if we've found the needle return
//    if (v === needle) {
//      return true
//    }

    // if the value is greated than the need.
    // move to the left half, i.e the part that has lower values
//    if (v > needle) {
//      hi = mid
//    } else {
      // if the value is less than the needle move to the right half, i.e the part with bigger values.
 //     lo = mid + 1;
//    }
//  }

//  return false;
}
